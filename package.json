{
  "name": "cypress-saucedemo-e2e",
  "version": "1.0.0",
  "description": "Cypress E2E tests for saucedemo.com",
  "scripts": {
    "cypress:open": "cypress open",
    "cypress:run": "cypress run",
    "cypress:report": "cypress run --reporter junit --reporter-options mochaFile=cypress/reports/result.xml",
    "test": "cypress run"
  },
  "devDependencies": {
    "cypress": "^13.6.2",
    "cypress-xpath": "^2.0.1"
  }
}
